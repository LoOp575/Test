{
  "name": "lq-short-hunter",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": "20.x"
  }
}
